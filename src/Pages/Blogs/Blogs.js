import React from 'react';
import Loading from '../../Components/Loading/Loading';

const Blogs = () => {
    return (
        <div>
            <Loading></Loading>
        </div>
    );
};

export default Blogs;